{
  "name": "@bettraction/types",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'Linting types...'",
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests for types package'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4.5"
  }
}
